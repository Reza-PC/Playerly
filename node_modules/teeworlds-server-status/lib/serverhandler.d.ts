import { IServerInfo } from './serverinfo';
export declare class ServerHandler {
    address: string;
    port: number;
    ignoreToken: boolean;
    timeout: number;
    constructor(address: string, port?: number, ignoreToken?: boolean, timeout?: number);
    requestInfo(): Promise<IServerInfo | null>;
    private sendRequest;
    private parsePacket;
    private unPackInt;
}
